const MIN_NUM = 1;
const MAX_NUM = 15;
const MIN_NUM_GCD = 2;
const MAX_NUM_GCD = 100;
const NUM_OF_ATTEMPTS = 3;
const OPERATORS = ['+', '-', '*'];

export {
  MAX_NUM,
  MIN_NUM,
  MIN_NUM_GCD,
  MAX_NUM_GCD,
  NUM_OF_ATTEMPTS,
  OPERATORS,
};
